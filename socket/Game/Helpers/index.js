const 	{ 
			Collision,
			RigidBody,
			Vector2d,
		} = require('./bodies');

const 	{
			RandomInt,
			Scale,
			Timer,
			TCOLORS,
		} = require('./functions')


module.exports = {
			Collision,
			RigidBody,
			Vector2d,
			RandomInt,
			Scale,
			Timer,
			Log,
}