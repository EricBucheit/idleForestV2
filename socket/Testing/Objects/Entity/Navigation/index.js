const Navigation = require('../../../../Game/Objects/Entity/Navigation');


class NavigationTest {
	constructor(settings) {
		location = settings.location || "home";
		destination = settings.destination || "home";
		direction = settings.direction || "north";
	}
}

module.exports = NavigationTest